import { ImageFilterPipePipe } from './image-filter-pipe.pipe';

describe('ImageFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
